name: CloudMart CI (No Docker)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run basic CI
        run: |
          echo "CloudMart CI running..."
          echo "No Docker, no secrets, no deployment."
          echo "This workflow always passes successfully."
